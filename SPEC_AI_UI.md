# SkyBlueEarthJapan 経費管理アプリ
## AIプロンプト定義 & UI文言仕様（Home中心）
Version: 0.1

---

## 1. 目的
- 音声/テキスト入力から取引（売上/経費）を構造化し、勘定科目を推定する
- 不足情報を対話で埋め、ユーザー最終確認→確定させる
- Home画面の導線を「迷わず使える」状態にする（モバイル前提）

---

## 2. AI設計（役割・トーン・原則）

### 2.1 AIの人格（Role）
- 役割：ユーザー専属の「経理秘書（経費・売上の記録係）」
- 目的：入力を省力化しつつ、誤記帳を防ぐ（必ず確認）
- トーン：
  - 丁寧、簡潔、業務寄り
  - 余計な雑談はしない
  - 絵文字は原則使わない（UI側アイコンで表現）

### 2.2 AIの禁止事項
- 税務判断の断定（例：「これは必ず経費です」）は禁止
- 法律/税制の断言は避け「一般的には」「概算」表現を使う
- 不明確な情報を勝手に補完して確定させない（確認を挟む）

### 2.3 対話の原則（Conversation Rules）
- 不足項目は **最小の質問** で埋める
- 1ターンで最大2問まで（負担軽減）
- 一度に多く聞かない。優先順位は以下：
  1) type（売上/経費）
  2) date（日付）
  3) amount（金額）
  4) description（摘要）
  5) vendor（取引先）
  6) account_title（勘定科目）
  7) payment_method（支払方法）
- 確定前に必ず「最終確認（Confirm）」を表示する

---

## 3. 取引データ仕様（アプリ内部の正）

### 3.1 取引モデル（Transaction）
```
- id: string (UUID) ※確定後に付与
- type: "expense" | "revenue"
- date: "YYYY-MM-DD"
- amount: number (integer)
- vendor: string (optional)
- description: string
- account_title: string
- payment_method: "cash" | "bank" | "card" | "other" | "" (optional)
- memo: string (optional)
- receipt_fileId: string (optional)
- receipt_url: string (optional)
- status: "draft" | "confirmed" | "void"
- ai_raw: string (入力原文)
- ai_confidence: number (0..1)
```

---

## 4. AI入出力仕様（JSON Schema）

### 4.1 AI入力（User Promptに埋め込む構造）
アプリはAIに以下のJSONを渡す。

```json
{
  "utterance": "昨日セブンで250円",
  "input_mode": "voice",
  "now": "2026-01-24",
  "timezone": "Asia/Tokyo",
  "context": {
    "business_name": "スカイブルーアースジャパン",
    "payment_methods": ["cash","bank","card","other"],
    "account_titles": [
      {"name":"消耗品費","keywords":["文房具","事務用品","備品","消耗品"]},
      {"name":"旅費交通費","keywords":["電車","バス","タクシー","高速","駐車場","ガソリン"]},
      {"name":"通信費","keywords":["携帯","電話","インターネット","プロバイダ"]},
      {"name":"会議費","keywords":["打ち合わせ","会議","コワーキング"]},
      {"name":"接待交際費","keywords":["会食","贈答","手土産"]},
      {"name":"雑費","keywords":["その他","不明"]},
      {"name":"売上高","keywords":["請求","入金","売上","報酬"]}
    ],
    "vendor_hints": [
      {"vendor":"セブンイレブン","default_account_title":"雑費"},
      {"vendor":"Amazon","default_account_title":"消耗品費"}
    ]
  }
}
```

> NOTE: account_titles はDB（MASTER_AccountTitles）から毎回生成してもよいが、
> MVPでは代表的な科目だけでも可。

---

### 4.2 AI出力（必ずJSON、追加テキスト禁止）

AIの返答は **必ず次の形式のJSONのみ** とする（UI側で文章化）。

```json
{
  "type": "expense",
  "fields": {
    "date": "2026-01-23",
    "amount": 250,
    "vendor": "セブンイレブン",
    "description": "飲食",
    "payment_method": "",
    "account_title_suggestions": [
      {"name":"雑費","confidence":0.55,"reason":"コンビニの少額支出で用途不明のため"},
      {"name":"会議費","confidence":0.25,"reason":"打ち合わせの飲食の可能性"},
      {"name":"消耗品費","confidence":0.20,"reason":"日用品購入の可能性"}
    ]
  },
  "missing_fields": ["payment_method"],
  "questions": [
    {"id":"q_payment","text":"支払方法は現金・口座・カードのどれですか？","choices":["cash","bank","card","other"]}
  ],
  "confidence": 0.62,
  "warnings": [
    {"code":"LOW_DESCRIPTION","text":"用途（摘要）が曖昧です。必要なら「何に使ったか」を一言追加してください。"}
  ]
}
```

#### 出力フィールドの要件

| フィールド | 要件 |
|------------|------|
| `type` | 必須（expense/revenue） |
| `fields.date` | 推定できなければ空文字 `""` にし、missing_fieldsに入れる |
| `fields.amount` | 推定できなければ `null` にし、missing_fieldsに入れる |
| `questions` | 最大2件まで |
| `account_title_suggestions` | 最大3件（必ずconfidence降順） |
| `confidence` | 0..1 |

---

## 5. AIプロンプト定義（System / Developer / User）

### 5.1 System Prompt（固定）

以下をシステムプロンプトとして使用する（アプリ側固定）。

```
あなたは個人事業の経理秘書です。
ユーザーの入力（音声の文字起こし/テキスト）から、取引（売上/経費）を構造化します。
必ずJSONのみで出力してください。余計な文章は一切出力しないでください。
不明な項目は推測で埋めず、missing_fields に入れ、最大2つまで質問してください。
税務判断の断定はしません。「一般的には」などの表現も出力しません（UI側で注意書きを出します）。
```

### 5.2 User Prompt（毎回生成）

アプリはAIに以下フォーマットで渡す（`{{input_json}}` は 4.1 のJSON）。

```
次の入力を取引として解釈し、指定のJSON形式で返してください。
{{input_json}}
```

### 5.3 Few-shot（任意・効果大）

MVPで精度を上げたい場合は、以下を例として追加してよい（JSONのみの厳密例）。

- 売上例：「1/10 〇〇社から報酬 100,000円 入金」
- 交通費例：「昨日 高速道路 765円」

---

## 6. Home画面 UI文言（実用コピー）

※家計簿っぽい文言を排除し、事業用の表現に統一する。

### 6.1 ナビゲーション

| 要素 | 文言 |
|------|------|
| タブ1 | `ホーム` |
| タブ2 | `履歴` |
| タブ3 | `分析` |
| 右上トグル | `AI: ON` / `AI: OFF` |

> OFF時：AI解析せず手入力フォームのみ使用（保険）

### 6.2 Home（入力）ヘッダー

| 要素 | 文言 |
|------|------|
| タイトル | `AI経理` |
| サブ | `取引を入力（音声 / 手入力）` |

### 6.3 音声入力エリア

| 要素 | 文言 |
|------|------|
| セクション見出し | `話して入力` |
| メインボタン | `話す` |
| ボタン状態（通常） | `話す` |
| ボタン状態（録音中） | `聞き取り中…（もう一度タップで停止）` |
| ボタン状態（解析中） | `AI解析中…` |
| 入力欄プレースホルダ | `ここに音声の内容が表示されます（編集もできます）` |
| 送信ボタン | `解析する` |

### 6.4 手入力導線

| 要素 | 文言 |
|------|------|
| ボタン | `手入力で登録する` |
| 説明 | `音声が難しい場合は手入力できます` |

### 6.5 AI解析結果カード

| 要素 | 文言 |
|------|------|
| 見出し | `AI解析結果` |
| 足りない項目がある場合 | `確認が必要です` |
| 質問表示 | `次を教えてください：{質問文}` |
| 候補提示 | `勘定科目候補：` |
| ボタン1 | `この内容で確認へ` |
| ボタン2 | `修正する` |
| ボタン3 | `破棄` |

### 6.6 Confirm（最終確認）画面（Home内のサブビュー）

| 要素 | 文言 |
|------|------|
| 見出し | `最終確認` |
| 説明 | `必要なら修正してから確定してください` |
| ボタン1 | `確定する` |
| ボタン2 | `戻る` |

### 6.7 登録完了トースト

| 要素 | 文言 |
|------|------|
| メイン | `登録しました` |
| サブ | `履歴で確認できます` |

---

## 7. UIの動作仕様（Home）

### 7.1 音声→テキスト

- Web Speech API 等で音声を文字起こしして `utterance` として保持
- ユーザーが送信前にテキストを編集できる

### 7.2 AI解析（チャット対話）

- AIが `questions` を返した場合：
  - UIで質問カードを表示
  - ユーザー回答を受け取り、`utterance` に追記 or `context` として再送
- 2回以内でConfirmへ誘導（長引かせない）

### 7.3 Confirm→確定

- Confirmで確定したら `confirmTransaction()` を呼ぶ
- 成功時：トースト表示→履歴へ導線（任意で自動遷移）

---

## 8. 実装チケット化しやすいタスク一覧（Home+AI）

| No. | タスク |
|-----|--------|
| 1 | Home UI（音声入力、テキスト編集、送信） |
| 2 | AI呼び出しラッパー（System/User prompt生成、JSONパース、バリデーション） |
| 3 | 質問UI（choices対応、最大2問） |
| 4 | Confirmサブビュー（編集可能フォーム） |
| 5 | confirmTransaction GAS API接続 |
| 6 | エラーUI（ネットワーク/JSON不正/必須項目欠落） |

---

## 9. エラーハンドリング仕様

| エラー状況 | 表示メッセージ |
|------------|----------------|
| AIがJSON以外を返した | `AIの出力形式が不正です。もう一度お試しください。` |
| missing_fields が date/amount/type のいずれかを含む | Confirmに進ませず、質問/手入力誘導 |
| GAS書き込み失敗 | `保存に失敗しました。通信状況をご確認ください。` + 再試行ボタン |

---

## 10. 受入条件（Home+AI）

- [ ] 音声入力→AI解析→質問→Confirm→確定が完走できる
- [ ] 売上/経費をAIが判定でき、判定できない場合は質問する
- [ ] Confirmでユーザーが修正できる
- [ ] 確定後に履歴で見える

---

## 11. 対話フロー例（詳細）

### 11.1 完全な入力（質問なし）

```
User: 昨日Amazonで12000円、事務用品を買った

AI Response:
{
  "type": "expense",
  "fields": {
    "date": "2026-01-23",
    "amount": 12000,
    "vendor": "Amazon",
    "description": "事務用品購入",
    "payment_method": "",
    "account_title_suggestions": [
      {"name":"消耗品費","confidence":0.85,"reason":"事務用品のため"}
    ]
  },
  "missing_fields": [],
  "questions": [],
  "confidence": 0.85,
  "warnings": []
}

UI表示:
┌─────────────────────────────────┐
│ AI解析結果                      │
├─────────────────────────────────┤
│ 種別：経費                      │
│ 日付：2026年1月23日             │
│ 金額：¥12,000                   │
│ 取引先：Amazon                  │
│ 内容：事務用品購入              │
│ 勘定科目候補：消耗品費（85%）   │
├─────────────────────────────────┤
│ [この内容で確認へ] [修正] [破棄]│
└─────────────────────────────────┘
```

### 11.2 情報不足（質問あり）

```
User: セブンで250円

AI Response:
{
  "type": "expense",
  "fields": {
    "date": "",
    "amount": 250,
    "vendor": "セブンイレブン",
    "description": "",
    "payment_method": "",
    "account_title_suggestions": [
      {"name":"雑費","confidence":0.50,"reason":"用途不明のため"}
    ]
  },
  "missing_fields": ["date", "description"],
  "questions": [
    {"id":"q_date","text":"いつの取引ですか？","choices":[]},
    {"id":"q_desc","text":"何に使いましたか？（例：飲食、日用品）","choices":[]}
  ],
  "confidence": 0.40,
  "warnings": []
}

UI表示:
┌─────────────────────────────────┐
│ AI解析結果                      │
├─────────────────────────────────┤
│ 確認が必要です                  │
│                                 │
│ 次を教えてください：            │
│ ・いつの取引ですか？            │
│ ・何に使いましたか？            │
│                                 │
│ [回答入力欄]                    │
│ [送信]                          │
└─────────────────────────────────┘
```

### 11.3 売上入力

```
User: 今日クライアントAから10万円入金があった

AI Response:
{
  "type": "revenue",
  "fields": {
    "date": "2026-01-24",
    "amount": 100000,
    "vendor": "クライアントA",
    "description": "入金",
    "payment_method": "bank",
    "account_title_suggestions": [
      {"name":"売上高","confidence":0.95,"reason":"報酬入金のため"}
    ]
  },
  "missing_fields": [],
  "questions": [],
  "confidence": 0.95,
  "warnings": []
}

UI表示:
┌─────────────────────────────────┐
│ AI解析結果                      │
├─────────────────────────────────┤
│ 種別：売上                      │
│ 日付：2026年1月24日             │
│ 金額：¥100,000                  │
│ 取引先：クライアントA           │
│ 内容：入金                      │
│ 勘定科目候補：売上高（95%）     │
├─────────────────────────────────┤
│ [この内容で確認へ] [修正] [破棄]│
└─────────────────────────────────┘
```

---

## 12. AI精度向上のための追加ルール

### 12.1 日付推定ルール

| 入力表現 | 推定結果 |
|----------|----------|
| 今日 | now |
| 昨日 | now - 1日 |
| 一昨日 | now - 2日 |
| 先週 | missing_fields（曖昧すぎる） |
| 1/24、1月24日 | 当年の1月24日 |
| 去年の12月 | missing_fields（日が不明） |

### 12.2 金額推定ルール

| 入力表現 | 推定結果 |
|----------|----------|
| 1万円 | 10000 |
| 1万2千円 | 12000 |
| 1.2万 | 12000 |
| 千円 | 1000 |
| 100円 | 100 |
| 10万 | 100000 |

### 12.3 売上/経費判定キーワード

| type | キーワード |
|------|------------|
| revenue | 入金、売上、報酬、請求、振込（受取） |
| expense | 買った、払った、支払い、購入、使った |

---

## 13. Confirm画面フォーム仕様

### フォームフィールド

| フィールド | 入力タイプ | 必須 | 備考 |
|------------|------------|------|------|
| 種別 | セグメント（売上/経費） | ○ | |
| 日付 | 日付ピッカー | ○ | |
| 金額 | 数値入力 | ○ | カンマ自動挿入 |
| 取引先 | テキスト | - | サジェスト付き |
| 内容（摘要） | テキスト | ○ | |
| 勘定科目 | プルダウン | ○ | 検索可能 |
| 支払方法 | セグメント | - | 現金/口座/カード/その他 |
| メモ | テキストエリア | - | |

### バリデーション

| チェック | エラーメッセージ |
|----------|------------------|
| 日付が空 | `日付を入力してください` |
| 金額が0以下 | `金額を正しく入力してください` |
| 内容が空 | `内容を入力してください` |
| 勘定科目が未選択 | `勘定科目を選択してください` |

---

## 14. 売上 / 経費の「言い方テンプレ」

ユーザーが迷わず音声入力できるよう、Home画面に **例文（ヒント）** を表示する。
AIは以下の言い回しを優先的に学習・解釈する。

### 14.1 経費の言い方テンプレ（Expense）

以下の要素が含まれると、AIは `type=expense` を強く推定する。

#### 基本形
- 「昨日、**セブンで250円**」
- 「**1月20日 Amazonで12,000円 事務用品**」
- 「**高速道路 765円**」
- 「**ガソリン代 5,000円**」

#### 補足あり
- 「**打ち合わせでカフェ 800円**」
- 「**移動で電車代 1,200円**」
- 「**仕事用の本 2,000円**」

#### 支払方法つき
- 「**カードで Amazon 12,000円**」
- 「**現金で コンビニ 150円**」

---

### 14.2 売上の言い方テンプレ（Revenue）

以下の要素が含まれると、AIは `type=revenue` を強く推定する。

#### 基本形
- 「**〇〇社から報酬 100,000円 入金**」
- 「**売上 50,000円**」
- 「**1月分 請求 200,000円**」

#### 入金明示
- 「**〇〇株式会社 入金 150,000円**」
- 「**報酬が振り込まれた 80,000円**」

#### 月次まとめ
- 「**今月の売上 300,000円**」（※まとめ入力も許可）

---

### 14.3 UI表示用ヒント文言（Home画面）

音声入力ボタンの下に以下を表示する。

> 例）
> ・昨日 セブンで250円
> ・Amazonで12,000円 事務用品
> ・〇〇社から報酬 100,000円 入金

※ヒントはランダムで切り替えてもよい。

---

## 15. 勘定科目の推定ルール（簡易ルール＋AI）

### 15.1 推定の基本方針

- **ルールベース → AI推定 → ユーザー確認** の三段構え
- ルールで確信度が高い場合でも **自動確定は禁止**
- 必ず候補提示＋Confirmを挟む

---

### 15.2 ルールベース推定（優先）

以下に該当する場合は、confidence を高めに設定する。

| キーワード例 | 勘定科目 | 初期confidence |
|---|---|---|
| 電車 / バス / タクシー / 高速 / 駐車場 | 旅費交通費 | 0.75 |
| 文房具 / 事務用品 / 備品 / Amazon | 消耗品費 | 0.70 |
| 携帯 / 電話 / インターネット | 通信費 | 0.80 |
| 打ち合わせ / 会議 / カフェ | 会議費 | 0.65 |
| 会食 / 手土産 / 接待 | 接待交際費 | 0.70 |
| 書籍 / 本 / 参考書 | 新聞図書費 | 0.70 |
| 不明 / コンビニ / 少額 | 雑費 | 0.55 |

※ vendor_hints（例：Amazon → 消耗品費）がある場合は +0.1

---

### 15.3 AI推定（補助）

- 文章全体の文脈から用途を推測
- ルールに該当しない場合は AIのみで候補生成
- 候補は最大3件、confidence降順

---

### 15.4 最終候補の生成ルール

| 条件 | 処理 |
|------|------|
| confidence >= 0.8 の候補が1件 | その候補を先頭表示 |
| 0.6〜0.79 が複数 | 2〜3件提示 |
| 0.6未満のみ | 雑費を必ず含める |

---

## 16. 質問の優先順位（最短で確定させる）

### 16.1 必須項目の定義

以下が揃わない場合は **Confirmへ進めない**。

1. type（売上 / 経費）
2. date（取引日）
3. amount（金額）
4. description（摘要）

---

### 16.2 質問優先順位

AIが質問を生成する際は、以下の順で不足を埋める。

#### 優先度A（最優先）

| 不足項目 | 質問 |
|----------|------|
| type が不明 | 「これは経費ですか？売上ですか？」 |
| amount が不明 | 「金額はいくらですか？」 |
| date が不明 | 「取引日はいつですか？」 |

#### 優先度B（次点）

| 不足項目 | 質問 |
|----------|------|
| description が曖昧 | 「用途を一言で教えてください」 |
| account_title が曖昧 | 「この中から選んでください」 |

#### 優先度C（余裕があれば）

| 不足項目 | 質問 |
|----------|------|
| payment_method | 「支払方法は何ですか？」 |
| vendor | 「取引先はどこですか？」 |

---

### 16.3 質問数制限ルール

- 1ターン最大2問まで
- 2ターンで必須項目が揃わない場合：
  - 手入力フォームを提示
  - 「手入力で続けますか？」と確認

---

### 16.4 質問UI文言テンプレ

| 項目 | 質問文 |
|------|--------|
| type | 「これは **経費** ですか？ **売上** ですか？」 |
| amount | 「金額はいくらですか？」 |
| date | 「取引日はいつですか？（例：1/20）」 |
| account_title | 「勘定科目は次のどれが近いですか？」 |

---

## 17. 最短確定フロー（理想）

AIとUIは以下の状態を目指す。

```
入力 → AI解析 → 質問0〜1回 → Confirm → 確定
```

- 質問2回以上は「例外」
- ユーザーの負担を最小化することを最優先とする

---

## 18. 実装チェックリスト（AI体験）

- [ ] 音声例文を見れば、初見でも入力できる
- [ ] 勘定科目候補が「なぜそうなるか」分かる
- [ ] 質問が多すぎない
- [ ] 確定までのステップが短い
- [ ] 手入力への逃げ道がある

---

## 19. 設計総括

### UI
- 既存家計簿UIを活かしつつ、事業用に昇華

### AI
- 推測しすぎず、確認を怠らない「経理秘書」

### UX
- 最短で確定、迷ったら人間に委ねる
